<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - InstaKilogram</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* --- Variables --- */
        :root {
            --bg-navy: #0a192f;
            --card-navy: #112240;
            --text-white: #e6f1ff;
            --text-gray: #8892b0;
            --accent-cyan: #64ffda;
            --btn-primary: #1d4ed8;
            --btn-danger: #ef4444;
            --border-color: rgba(255, 255, 255, 0.1);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-navy);
            color: var(--text-white);
            min-height: 100vh;
            padding-bottom: 50px;
        }

        /* --- Navbar --- */
        nav {
            background-color: rgba(17, 34, 64, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--border-color);
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .brand {
            font-size: 1.3rem;
            font-weight: 700;
            background: linear-gradient(90deg, #e6f1ff, #64ffda);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .btn-logout {
            background-color: transparent;
            border: 1px solid var(--btn-danger);
            color: var(--btn-danger);
            padding: 8px 20px;
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s;
        }

        .btn-logout:hover {
            background-color: var(--btn-danger);
            color: white;
        }

        /* --- Container --- */
        .container {
            max-width: 1200px;
            margin: 30px auto;
            padding: 0 20px;
        }

        /* --- Stats Cards (Bonus Visual) --- */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background-color: var(--card-navy);
            padding: 20px;
            border-radius: 12px;
            border: 1px solid var(--border-color);
            display: flex;
            flex-direction: column;
        }

        .stat-label { color: var(--text-gray); font-size: 0.9rem; margin-bottom: 5px; }
        .stat-value { font-size: 2rem; font-weight: 700; color: var(--accent-cyan); }

        /* --- Section Titles --- */
        h3 {
            margin-bottom: 20px;
            font-weight: 600;
            color: var(--text-white);
            border-left: 4px solid var(--btn-primary);
            padding-left: 15px;
        }

        /* --- Table Styling --- */
        .table-container {
            background-color: var(--card-navy);
            border-radius: 12px;
            border: 1px solid var(--border-color);
            overflow-x: auto; /* Agar scrollable di HP */
            margin-bottom: 40px;
            box-shadow: 0 10px 30px -10px rgba(2, 12, 27, 0.5);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            white-space: nowrap;
        }

        th, td {
            padding: 16px 20px;
            text-align: left;
            border-bottom: 1px solid var(--border-color);
        }

        th {
            background-color: rgba(255, 255, 255, 0.02);
            color: var(--text-gray);
            font-weight: 600;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        tr:last-child td { border-bottom: none; }
        tr:hover td { background-color: rgba(255, 255, 255, 0.02); }

        /* --- Badges & Images --- */
        .badge {
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
        }

        .badge-admin {
            background-color: rgba(100, 255, 218, 0.1);
            color: var(--accent-cyan);
            border: 1px solid var(--accent-cyan);
        }

        .badge-user {
            background-color: rgba(136, 146, 176, 0.1);
            color: var(--text-gray);
            border: 1px solid var(--text-gray);
        }

        .post-thumb {
            width: 60px;
            height: 60px;
            border-radius: 8px;
            object-fit: cover;
            border: 1px solid var(--border-color);
        }

        /* --- Action Buttons --- */
        .btn-delete {
            background-color: rgba(239, 68, 68, 0.1);
            color: #fca5a5;
            padding: 6px 14px;
            text-decoration: none;
            border-radius: 6px;
            font-size: 0.85rem;
            font-weight: 500;
            transition: all 0.2s;
            border: 1px solid transparent;
            display: inline-flex;
            align-items: center;
            gap: 5px;
        }

        .btn-delete:hover {
            background-color: var(--btn-danger);
            color: white;
            border-color: var(--btn-danger);
        }
        
        .no-action {
            color: var(--text-gray);
            font-size: 0.9rem;
            font-style: italic;
        }

    </style>
</head>
<body>

    <nav>
        <div class="brand">Admin Dashboard</div>
        <a href="/logout" class="btn-logout">Logout</a>
    </nav>

    <div class="container">
        
        <div class="stats-grid">
            <div class="stat-card">
                <span class="stat-label">Total Users</span>
                <span class="stat-value"><%= users.length %></span>
            </div>
            <div class="stat-card">
                <span class="stat-label">Total Posts</span>
                <span class="stat-value"><%= posts.length %></span>
            </div>
        </div>

        <h3>User Management</h3>
        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Email</th>
                        <th>Full Name</th>
                        <th>Role</th>
                        <th style="text-align: center;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(u => { %>
                    <tr>
                        <td><%= u.email %></td>
                        <td>
                            <span style="font-weight: 500; color: white;">
                                <%= u.UserProfile ? u.UserProfile.name : '-' %>
                            </span>
                        </td>
                        <td>
                            <% if(u.role === 'admin') { %>
                                <span class="badge badge-admin">Admin</span>
                            <% } else { %>
                                <span class="badge badge-user">User</span>
                            <% } %>
                        </td>
                        <td style="text-align: center;">
                            <% if(u.role !== 'admin') { %>
                                <a href="/admin/delete/<%= u.id %>" 
                                   onclick="return confirm('Yakin ingin menghapus user <%= u.email %>? Data profil dan postingan akan hilang.')" 
                                   class="btn-delete">
                                   Delete
                                </a>
                            <% } else { %>
                                <span class="no-action">Protected</span>
                            <% } %>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

        <h3>Content Management</h3>
        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Image</th>
                        <th>Caption</th>
                        <th>Posted By</th>
                        <th style="text-align: center;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% posts.forEach(p => { %>
                    <tr>
                        <td>
                            <img src="<%= p.posting %>" alt="Post Img" class="post-thumb">
                        </td>
                        <td style="max-width: 300px; white-space: normal;">
                            <%= p.caption %>
                        </td>
                        <td>
                            <%= p.UserProfile ? p.UserProfile.name : 'Unknown' %>
                        </td>
                        <td style="text-align: center;">
                            <a href="/admin/deletePost/<%= p.id %>" 
                               onclick="return confirm('Yakin ingin menghapus postingan ini?')" 
                               class="btn-delete">
                               Delete
                            </a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

    </div>

</body>
</html>