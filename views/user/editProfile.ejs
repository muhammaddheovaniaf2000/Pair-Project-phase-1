<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile - InstaKilogram</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        /* --- CSS Variables (Konsisten) --- */
        :root {
            --bg-navy: #0a192f;
            --card-navy: #112240;
            --input-bg: #172a45;
            --text-white: #e6f1ff;
            --text-gray: #8892b0;
            --btn-primary: #1d4ed8;
            --btn-danger: #ef4444;
            --border-color: rgba(255, 255, 255, 0.1);
            --accent-cyan: #64ffda;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-navy);
            color: var(--text-white);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        /* --- Container Kartu --- */
        .edit-card {
            background-color: var(--card-navy);
            padding: 2.5rem;
            border-radius: 16px;
            box-shadow: 0 10px 30px -10px rgba(2, 12, 27, 0.7);
            width: 100%;
            max-width: 500px;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        h2 {
            text-align: center;
            margin-bottom: 2rem;
            font-weight: 600;
            color: var(--text-white);
        }

        /* --- Foto Profil Preview --- */
        .avatar-preview {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            margin: 0 auto 1.5rem;
            display: block;
            object-fit: cover;
            border: 3px solid var(--btn-primary);
            background-color: var(--input-bg);
        }

        /* --- Form Styles --- */
        .form-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: var(--text-gray);
            font-size: 0.9rem;
            font-weight: 500;
        }

        input {
            width: 100%;
            padding: 12px 15px;
            background-color: var(--input-bg);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            color: var(--text-white);
            font-family: inherit;
            font-size: 0.9rem;
            outline: none;
            transition: all 0.3s ease;
        }

        input:focus {
            border-color: var(--btn-primary);
            box-shadow: 0 0 0 2px rgba(29, 78, 216, 0.2);
        }

        /* --- Button Group --- */
        .btn-group {
            display: flex;
            gap: 15px;
            margin-top: 10px;
        }

        .btn {
            padding: 12px;
            border-radius: 8px;
            font-size: 0.95rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            text-align: center;
            border: none;
            flex: 1; /* Membagi lebar tombol 50:50 */
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Tombol Save (Primary) */
        .btn-success {
            background-color: var(--btn-primary);
            color: white;
        }

        .btn-success:hover {
            background-color: #2563eb;
            transform: translateY(-2px);
        }

        /* Tombol Cancel (Secondary/Outline) */
        .btn-danger {
            background-color: transparent;
            color: var(--text-gray);
            border: 1px solid var(--border-color);
        }

        .btn-danger:hover {
            border-color: var(--btn-danger);
            color: var(--btn-danger);
            background-color: rgba(239, 68, 68, 0.05);
        }

        /* Helper Text */
        .help-text {
            font-size: 0.75rem;
            color: var(--text-gray);
            margin-top: 5px;
            opacity: 0.7;
        }
    </style>
</head>
<body>

    <div class="edit-card">
        <h2>Edit Profile</h2>
        
        <img src="<%= userProfile.profilePict ? userProfile.profilePict : 'https://via.placeholder.com/150' %>" 
             class="avatar-preview" 
             alt="Current Profile Picture"
             onerror="this.src='https://via.placeholder.com/150'">

        <form action="/user/<%= userProfile.UserId %>/edit" method="post">
            
            <div class="form-group">
                <label>Full Name</label>
                <input type="text" name="name" value="<%= userProfile.name %>" required autocomplete="off">
            </div>

            <div class="form-group">
                <label>Profile Picture URL</label>
                <input type="text" name="profilePict" value="<%= userProfile.profilePict %>" placeholder="https://example.com/myphoto.jpg" autocomplete="off">
                <p class="help-text">Paste a valid image link (URL) here.</p>
            </div>

            <div class="btn-group">
                <a href="/user/<%= userProfile.UserId %>" class="btn btn-danger">Cancel</a>
                
                <button type="submit" class="btn btn-success">Update Profile</button>
            </div>

        </form>
    </div>

</body>
</html>