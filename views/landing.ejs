<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>InstaKilogram</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

    <style>
        /* --- CSS Variables untuk Skema Warna --- */
        :root {
            --bg-navy: #0a192f;       /* Biru Dongker Gelap */
            --card-navy: #112240;     /* Biru Dongker Sedikit Terang */
            --accent-blue: #64ffda;   /* Aksen Cyan/Tosca (Opsional untuk modern look) */
            --text-white: #e6f1ff;    /* Putih lembut */
            --text-gray: #8892b0;     /* Abu-abu untuk teks sekunder */
            --btn-primary: #1d4ed8;   /* Biru tombol */
        }

        /* --- Reset Dasar --- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-navy);
            color: var(--text-white);
            height: 100vh;
            display: flex;
            justify-content: center; /* Posisi Tengah Horizontal */
            align-items: center;     /* Posisi Tengah Vertikal */
            overflow: hidden;
        }

        /* --- Container Kartu --- */
        .login-card {
            background-color: var(--card-navy);
            padding: 3rem;
            border-radius: 16px;
            box-shadow: 0 10px 30px -10px rgba(2, 12, 27, 0.7);
            text-align: center;
            width: 100%;
            max-width: 400px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: transform 0.3s ease;
        }

        .login-card:hover {
            transform: translateY(-5px);
        }

        /* --- Typography --- */
        h1 {
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            letter-spacing: 1px;
            background: linear-gradient(90deg, #fff, #94a3b8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        p {
            color: var(--text-gray);
            font-size: 0.95rem;
            margin-bottom: 2.5rem;
            font-weight: 300;
        }

        /* --- Button Container --- */
        .action-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        /* --- Styling Link menjadi Tombol --- */
        a {
            text-decoration: none;
            padding: 12px 20px;
            border-radius: 8px;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s ease;
            display: inline-block;
            width: 100%;
        }

        /* Tombol Login (Primary) */
        .btn-login {
            background-color: var(--btn-primary);
            color: white;
            box-shadow: 0 4px 14px 0 rgba(29, 78, 216, 0.39);
        }

        .btn-login:hover {
            background-color: #2563eb;
            transform: scale(1.02);
        }

        /* Tombol Register (Secondary/Outline) */
        .btn-register {
            background-color: transparent;
            color: var(--text-white);
            border: 1px solid var(--text-gray);
        }

        .btn-register:hover {
            border-color: var(--text-white);
            background-color: rgba(255, 255, 255, 0.05);
        }

        /* Pemisah visual kecil */
        .divider {
            margin: 10px 0;
            font-size: 0.8rem;
            color: var(--text-gray);
            display: none; /* Disembunyikan untuk tampilan minimalis */
        }

    </style>
</head>

<body>

    <div class="login-card">
        <div style="width: 50px; height: 50px; background: linear-gradient(45deg, #1d4ed8, #64ffda); margin: 0 auto 20px; border-radius: 12px;"></div>

        <h1>InstaKilogram</h1>
        <p>Share your moments.</p>
        
        <div class="action-buttons">
            <a href="/login" class="btn-login">Login</a>
            <a href="/login/regist" class="btn-register">Register Account</a>
        </div>
    </div>

</body>
</html>